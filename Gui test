repeat task.wait() until game:IsLoaded()

local CorrectKey = "SL-ML-2026"

-- GUI
local Gui = Instance.new("ScreenGui", game.CoreGui)
Gui.Name = "SL_KEY_UI"

local Main = Instance.new("Frame", Gui)
Main.Size = UDim2.new(0,700,0,350)
Main.Position = UDim2.new(0.5,-350,0.5,-175)
Main.BackgroundColor3 = Color3.fromRGB(15,15,15)
Main.Active = true
Main.Draggable = true
Instance.new("UICorner", Main)

local Stroke = Instance.new("UIStroke", Main)
Stroke.Color = Color3.fromRGB(255,0,0)
Stroke.Thickness = 3

-- LEFT SIDE
local Left = Instance.new("Frame", Main)
Left.Size = UDim2.new(0.45,0,1,0)
Left.BackgroundTransparency = 1

local Title = Instance.new("TextLabel", Left)
Title.Size = UDim2.new(1,0,0,60)
Title.BackgroundTransparency = 1
Title.Text = "🔥 SL HUB 🔥"
Title.Font = Enum.Font.GothamBold
Title.TextSize = 22
Title.TextColor3 = Color3.fromRGB(255,80,80)

local Features = Instance.new("TextLabel", Left)
Features.Size = UDim2.new(1,-20,1,-70)
Features.Position = UDim2.new(0,10,0,60)
Features.BackgroundTransparency = 1
Features.Text = "R ⚡ FAST\nG 🛡 SAFE\nP 👑 GOOD SCRIPTS\n💡 Easy to use\n⚡ Fast Execution\n🚀 No lag\n👻 MADE BY SL_GHOST"
Features.TextColor3 = Color3.fromRGB(255,255,255)
Features.TextXAlignment = Enum.TextXAlignment.Left
Features.TextYAlignment = Enum.TextYAlignment.Top

-- RIGHT SIDE
local Right = Instance.new("Frame", Main)
Right.Size = UDim2.new(0.55,0,1,0)
Right.Position = UDim2.new(0.45,0,0,0)
Right.BackgroundTransparency = 1

local KeyTitle = Instance.new("TextLabel", Right)
KeyTitle.Size = UDim2.new(1,0,0,60)
KeyTitle.BackgroundTransparency = 1
KeyTitle.Text = "🔑 KEY SYSTEM"
KeyTitle.TextColor3 = Color3.fromRGB(255,80,80)

local Box = Instance.new("TextBox", Right)
Box.Size = UDim2.new(0.8,0,0,45)
Box.Position = UDim2.new(0.1,0,0.25,0)
Box.PlaceholderText = "Paste key here"
Box.BackgroundColor3 = Color3.fromRGB(25,25,25)
Box.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", Box)

local Verify = Instance.new("TextButton", Right)
Verify.Size = UDim2.new(0.8,0,0,45)
Verify.Position = UDim2.new(0.1,0,0.38,0)
Verify.Text = "VERIFY"
Verify.BackgroundColor3 = Color3.fromRGB(255,0,0)
Verify.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", Verify)

local Discord = Instance.new("TextButton", Right)
Discord.Size = UDim2.new(0.8,0,0,45)
Discord.Position = UDim2.new(0.1,0,0.51,0)
Discord.Text = "DISCORD"
Discord.BackgroundColor3 = Color3.fromRGB(255,0,0)
Discord.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", Discord)

local GetKey = Instance.new("TextButton", Right)
GetKey.Size = UDim2.new(0.8,0,0,45)
GetKey.Position = UDim2.new(0.1,0,0.64,0)
GetKey.Text = "GET KEY"
GetKey.BackgroundColor3 = Color3.fromRGB(255,0,0)
GetKey.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", GetKey)

-- MESSAGE LABEL
local MsgLabel = Instance.new("TextLabel", Right)
MsgLabel.Size = UDim2.new(0.8,0,0,30)
MsgLabel.Position = UDim2.new(0.1,0,0.8,0)
MsgLabel.BackgroundTransparency = 1
MsgLabel.TextColor3 = Color3.fromRGB(255,255,255)
MsgLabel.Visible = false

-- SCRIPTS
local Scripts = Instance.new("Frame", Right)
Scripts.Size = UDim2.new(1,0,0.55,0)
Scripts.Position = UDim2.new(0,0,0.2,0)
Scripts.BackgroundTransparency = 1
Scripts.Visible = false

local Layout = Instance.new("UIListLayout", Scripts)
Layout.Padding = UDim.new(0,6)

local function NewScript(name,link)
    local btn = Instance.new("TextButton", Scripts)
    btn.Size = UDim2.new(0.8,0,0,40)
    btn.Text = name
    btn.BackgroundColor3 = Color3.fromRGB(255,0,0)
    btn.TextColor3 = Color3.fromRGB(255,255,255)
    Instance.new("UICorner", btn)

    btn.MouseButton1Click:Connect(function()
        Gui.Enabled = false
        loadstring(game:HttpGet(link))()
    end)
end

-- ANTI AFK RAW
local Anti = Instance.new("TextButton", Scripts)
Anti.Size = UDim2.new(0.8,0,0,40)
Anti.Text = "ANTI AFK"
Anti.BackgroundColor3 = Color3.fromRGB(255,0,0)
Anti.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", Anti)

Anti.MouseButton1Click:Connect(function()
    Anti.Text = "LOADING..."
    loadstring(game:HttpGet("ANTI_AFK_RAW_LINK"))()
    Anti.Text = "ANTI AFK LOADED"
end)

-- GAME SCRIPTS
NewScript("Muscle Legends","https://raw.githubusercontent.com/XMASFIRE/SL-HUB-D/refs/heads/main/SL%20HUB%20ML%20(1).txt")
NewScript("Blox Fruits","https://raw.githubusercontent.com/XMASFIRE/SL-HUB-D/refs/heads/main/BloxFruits")
NewScript("Legends Of Speed","https://raw.githubusercontent.com/XMASFIRE/SL-HUB-D/refs/heads/main/SL%20HUB%20LOS")
NewScript("Escape Tsunami","https://raw.githubusercontent.com/XMASFIRE/SL-HUB-D/refs/heads/main/Tusnammi.txt")

-- VERIFY KEY
Verify.MouseButton1Click:Connect(function()
    if Box.Text == CorrectKey then
        MsgLabel.Text = "Key verified ✅"
        MsgLabel.Visible = true
        task.wait(1)
        MsgLabel.Visible = false

        Box.Visible = false
        Verify.Visible = false
        Discord.Visible = false
        GetKey.Visible = false
        KeyTitle.Text = "🔥 SCRIPTS"
        Scripts.Visible = true
    else
        MsgLabel.Text = "Wrong key ❌"
        MsgLabel.Visible = true
        task.wait(1)
        MsgLabel.Visible = false
    end
end)

-- GET KEY
GetKey.MouseButton1Click:Connect(function()
    setclipboard("KEY LINK")
    MsgLabel.Text = "Key link copied ✅"
    MsgLabel.Visible = true
    task.wait(1)
    MsgLabel.Visible = false
end)

-- DISCORD
Discord.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/yourserver")
    MsgLabel.Text = "Discord copied ✅"
    MsgLabel.Visible = true
    task.wait(1)
    MsgLabel.Visible = false
end)
